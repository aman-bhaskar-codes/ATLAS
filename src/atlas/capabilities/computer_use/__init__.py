"""Computer-use fabric — ONE logical COMPUTER_USE capability, many bodies.

The reasoning core speaks PerceptionSnapshot/ControlAction; substrate adapters
(browser, macOS, Android) translate. Nothing in this package may bypass the
SafetyEngine — ComputerUseTool exists precisely so every action funnels
through guard() like any other tool (Phase 15/48).
"""
